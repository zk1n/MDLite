# MDLite — Agent instructions

## 対象と優先事項

MDLiteはWindows 11 x64向け、日本語UIの軽量Workspace Markdown Editor。
利用者本文を守ること、操作性、省メモリ、省CPU、保守性を優先する。
通常1〜6文書の編集で50MB以下を目標とするが、大容量や画像に合わせて機能を削らない。
Markdownの正本はソース。表示変更だけで本文を書き換えない。

初回はHumanが明示したTask Inboxの指示と同梱仕様を読む。
通常作業はリポジトリに取り込まれた合意版仕様・設計・受入条件を参照する。
資料版、ユーザーの追加指示、実装済み／未検証を区別する。

## 初回の実行設定

作業開始時に実際のrepo root、branch、worktree、未コミット変更、実効model／reasoningを確認する。
モデルを推測して記録しない。既存のユーザー共通設定・Skillを破壊せず利用する。
ローカル運用に`../.codex-shared/runtime/routing-core.md`が存在し、Humanがその共通設定の利用を指示した場合、
本ファイルからの相対パスとして読み、そこが指定する現行canonical policyを参照する。
親GitリポジトリのAGENTS.mdやconfigが自動継承されたとは仮定しない。

共通設定の配布が必要なら、既存のREADME・同期／導入スクリプトを検査して最小限に適用する。
他ProjectのAGENTS.md、.codex、認証、履歴、Git配信規則をディレクトリ単位でコピーしない。
実効設定の再読込に再起動が必要なら隠さず扱う。未適用の設定を有効と報告しない。
設定不足時は不明なchild routeを使わず、実効設定が明確なRootで可能な作業を継続する。
既存共通policyのRoot-only spawn等を尊重し、存在しないroleやfallbackを捏造しない。

## 実装

C++20／CMake／MSVCと現行設計を出発点にする。より軽量・保守容易な代替や実装障害に応じ変更できる。
変更理由と試験を記録し、技術選定の都合で必須のLive編集・表・画像・IMEを削らない。
WebView、JavaScript/Node.js実行環境、汎用プラグインは製品に導入しない。
Ponytailは開発時レビューとして使い、製品ランタイムへ含めない。
必要な部品境界は置くが、未要求の汎用フレームワークを作らない。

内部ゲート→実装→統合→試験まで、一般的な細部の確認待ちを繰り返さず続ける。
不明な細部は安全な既定値で補完し記録する。契約、本人認証、権限不足、不可逆な破壊だけを切り分ける。
不可能な外部準備で無関係の開発まで止めない。
雛形、TODO、無反応のUI、実測していない性能を完成扱いにしない。

## データと安全性

Workspace保存先は`.mdlite`。共有設定と、Git対象外の`.cache`／`.state`を分離する。
復旧本文をキャッシュ清掃で削除しない。AppDataへWorkspace本文・検索DBを退避しない。
実ノート、認証、秘密鍵、私的ログ、Task Inbox原文を公開する追跡ファイルへコピーしない。
公開資料・テスト・サンプルは個人パスや私的接続先に依存させない。
ツールの実行時検出と、公開設定に絶対パスを埋め込むことを区別する。

## Git

ローカルGitを正本にし、同じコミットを設定済みの2 remoteへ通常pushする。公開投影は作らない。
remote名は`origin`と`github`を運用上の標準とし、実URLをgit configから検査する。
開発はdevelopからfeature/*またはfix/*を作る。検証後にdevelopへ統合する。
mainは初期化用資料とリリースのみ。初回実装でmainへ統合、releaseタグ作成、公開Release作成をしない。
正式リリースはHumanが指示したrelease/*で確認後、main統合・タグ・developへの戻しを行う。
force、mirror、無断reset、履歴書換え、remote自動pull／mergeを行わない。
push後は両側の対象refが同じSHAであることを検証する。片方失敗を成功と報告せず、成功側を巻き戻さない。
エージェント別worktreeは独立作業の利点がある場合だけ使い、同一worktreeを並行commitしない。

Authorには実際の担当モデルを識別できる名前を使用し、AI-Model／AI-Agent／AI-Roleをcommit trailerに残す。
既存の公開可能な作者規約を先に確認する。Author/Committer変更はcommit単位とし、global設定を変更しない。
人間の初期設定commitは人間の名義でよい。AIが作成・変更した箇所を人間単独実装と偽らない。
認証アカウントとcommit Authorは別であり、AI名義の認証アカウントを作る必要はない。

## 開発環境と署名

MSVC、Visual Studio IDE、VS Code拡張の導入はユーザーが確認済み。
実装担当は実環境の検出を行うが、重複インストールを要求しない。
F5でビルド・デバッグ起動し、Release性能確認の入口を別に用意する。
実行ファイルの署名方式はリリース時まで保留。署名契約・証明書調達の調査を初回実装へ持ち込まない。
署名なしのローカル試験・ビルド・検証を許容し、公開バイナリの配布は行わない。
この保留はGitコミット／タグ署名の必須化とは別である。

## 完了

受入条件と実装箇所／試験／実測を対応させる。Ponytailレビューと正しさ・安全性レビューは別軸で行う。
実際に未使用のSkillや未実施テストを実施済みと報告しない。
HANDOFFは対象repo自身のローカル領域に作成し、登録済みの既存受渡しツールで処理する。
他Projectへのfallback、機密ログの公開、HANDOFFアップロード失敗を理由とした破壊的やり直しを禁止する。
